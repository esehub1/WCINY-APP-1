# wciny

## Development Environment

This repository targets an older Angular build toolchain that is compatible with Node 16/18. For local development we recommend using Node 18 to avoid OpenSSL3-related build issues.

- See `DEV_ENV.md` for recommended setup steps for Windows and macOS/Linux.
